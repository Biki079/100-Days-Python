# Exercise
# Input the two digits and display the output by adding this input digits.

a = input("Enter two digits")
first_digit = int (a[0])
second_digit = int (a[1])
print (first_digit + second_digit)
